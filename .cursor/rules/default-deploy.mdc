---
description: 完成网站修改后默认提交并推送到 GitHub
alwaysApply: true
---

# 默认部署约定

- 完成并验证用户要求的网站修改后，默认创建聚焦的 Conventional Commit，并推送到 `origin` 当前分支。
- Commit 描述使用简体中文，不超过 100 个字符。
- 不提交与当前任务无关的文件、密钥、环境变量或私人数据。
- 如果检查失败、远端冲突、需要破坏性 Git 操作，或用户明确要求只保留本地，则停止自动推送并说明原因。
